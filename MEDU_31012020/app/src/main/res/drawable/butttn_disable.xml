<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle" >

    <!-- <gradient
        android:angle="45"
        android:endColor="@color/disable_btn"
        android:centerColor="@color/disable_btn"
        android:startColor="@color/disable_btn"
        android:type= "linear" />-->

    <gradient
        android:startColor="@color/disable_btn"
        android:endColor="@android:color/transparent"
        android:angle="90" />

    <corners android:radius="5dp" />

    <stroke
        android:width="1dp"
        android:color="@color/grey" />

    <!--  <padding
        android:left="15dp"
        android:top="15dp"
        android:right="15dp"
        android:bottom="15dp" />
 -->
</shape>